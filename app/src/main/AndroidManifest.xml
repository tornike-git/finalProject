<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ChoiceActivity"></activity>
        <activity android:name=".wordsDisplayActivity" />
        <activity android:name=".WordsEnterActivity" />
        <activity android:name=".RandActivityDisplay" />
        <activity
            android:name=".WordActivity2"
            android:label="@string/title_activity_word2"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity android:name=".WordsActivity" />
        <activity android:name=".RandAcivity2" />
        <activity android:name=".RandActivity" />
        <activity android:name=".choiceActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".logInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>